spring:
  datasource:
    url: jdbc:mariadb://tool-db:3306/tools
    username: root
    password: ${DB_PASSWORD}
    driver-class-name: org.mariadb.jdbc.Driver
  data:
    redis:
      host: tool-redis
      port: 6379
      password: ${REDIS_PASSWORD}
logging:
  config: classpath:log/log-back.xml
